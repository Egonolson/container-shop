# Copy to .env.local. For local dev, run `supabase start` at the repo root
# and copy the printed "Project URL" and "Publishable" key below.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
